# Command reference

`$docs help` lists the interface. The commands are read-only unless their contract says otherwise:

- `context <task>` — bounded recall with sources, constraints, risks, and unloaded material.
- `map` — documentation topology, hot path, and source-of-truth relationships.
- `classify` — likely audience and Diátaxis type.
- `check` — links, anchors, reachability, duplicate titles, and hot-path bytes.
- `audit [scope]` — numbered, evidence-backed findings.
- `write <need>` / `update <change>` — one focused, evidence-verified documentation change.
- `init`, `migrate`, `cleanup` — exact preview first; a later approval is required before structural mutation.
- `fix <IDs|scope>` — revalidate and repair only explicitly selected findings.

Unknown or missing commands return help without side effects.

