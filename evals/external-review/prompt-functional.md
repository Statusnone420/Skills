# Synthetic functional-test prompt

Run the supplied deterministic synthetic cases against the repository skill/checker and adapters. Keep the run read-only and offline. Capture exact visible inputs, outputs, exit codes, file/line evidence, and any diff (expected empty unless the case says otherwise). Do not request hidden reasoning or expose secrets/private paths; distinguish observed result from inference.
