Activate the shared docs skill and run `cleanup` with the user's raw trailing text. This generic web prompt has no guaranteed filesystem, shell, or repository-tool capabilities; report unavailable tools honestly.
